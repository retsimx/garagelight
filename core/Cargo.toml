[package]
name = "garagelight-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "garagelight_core"
path = "src/lib.rs"

[dependencies]
sha2 = { workspace = true }

[dev-dependencies]
toml = "0.8"
serde = { version = "1", features = ["derive"] }
pollster = "0.4"
